BOT_TOKEN=your_bot_token_here
WEBAPP_URL=https://your-ngrok-url.ngrok-free.app

# Локальная разработка вне Telegram (в браузере).
# В проде НЕ выставлять — иначе любой запрос пойдёт от имени DEV_USER_ID.
DEV_MODE=false
DEV_USER_ID=0